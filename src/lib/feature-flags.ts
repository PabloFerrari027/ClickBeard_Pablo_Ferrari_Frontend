/**
 * The 4 routes marked [PLANNED] in the spec (administrative cancellation with
 * reason, barber unavailability CRUD) are now confirmed deployed to production
 * (spec §1, Appendix item 9).
 */
export const PLANNED_FEATURES_ENABLED = true;
